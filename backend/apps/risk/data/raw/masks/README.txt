Combined flood-mask set from all uploaded batches.
Exact duplicate uploads were consolidated; one binary PNG is provided per unique source JPG.
White (255) = interpreted flood/flood-impact region. Black (0) = background.
For before/after composites, the pre-flood panel is left black and the flood/after panel is masked.
These are approximate visual masks for dataset preparation/review, not survey-grade annotations.
